ADMIN_EMAIL=atmwithnopin@gmail.com
ADMIN_PASSWORD_HASH=
# Optional: set ADMIN_PASSWORD instead of ADMIN_PASSWORD_HASH for local dev.
ADMIN_PASSWORD=14Ajayanish
# Optional: Cloudinary upload support for persistent image storage.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_UPLOAD_PRESET=
# OpenAI — REQUIRED for the AI Poker Personality / AI Chronicle / "Generate Profile" features.
# Without it those buttons return a clear "AI generation is temporarily unavailable" message
# (the rest of the site works normally). Set this in the Railway service variables in production.
OPENAI_API_KEY=
PORT=3000
